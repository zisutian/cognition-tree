# Dark Modern 样板与验证记录

基线：`80ab630`。本轮先完成真实笔记和 Provider 设置样板，在 1280×720、1440×900 下验证并交由用户确认，然后继续其余页面。共享基础直接应用于生产调用方，没有独立演示版或主题开关。

样板已于 2026-09-09 获得用户确认。随后完成其余设置表单的纵向布局、枚举下拉框及成员复选框组；开发迁至独立 repo-dev，最终验收结果在后续记录中列出。

## 职责与迁移清单

| 组件族 | 基线中的重复路径 | 当前所有者及调用方 | 验证场景 |
|---|---|---|---|
| 按钮 | 活动栏、列表、树、菜单、ChoiceGroup、ToggleButton 各自渲染 button | Button 拥有普通 React 原生按钮；语义组件组合它并拥有选择、快捷键、拖放行为 | 点击、键盘、焦点、禁用、树重命名与菜单 |
| 复选框 | Todo 详情和周期表单直接渲染 input | CheckboxControl 拥有普通 React checkbox；Todo 与 Provider 使用它 | 勾选、周期选择、草稿保护 |
| 编辑器 checkbox | DOM widget 独立定义尺寸与颜色 | 共享视觉契约；CodeMirror 仅保留宿主位置、事件与文档命令 | 编辑器任务切换及既有撤销回归 |
| 表单 | 紧凑表单只有标签列；工具面板重复覆盖控件尺寸 | FormLayout 显式 columns/stacked；FieldRow 拥有标签、说明与错误关联 | Provider 纵向对齐、长值、保存/放弃 |
| 标题与目录 | AppFrame 单独实现侧栏标题；笔记单独排列目录动作 | PanelHeader 拥有面板标题；CompactContextGroupHeader 拥有目录标题与动作 | 标题固定、动作不遮挡、目录折叠 |
| 状态与问题 | Problems 头部同时承担底栏状态 | 控制器拥有唯一投影，StatusBar 与 ProblemsPanel 分别展示 | 全宽底栏、持续草稿消息、问题筛选与尺寸 |

原生控件所有权检查已覆盖共享组件内部，Todo 原生控件文件例外已删除。检查继续使用完整生产扫描与现有模块依赖规则。

## 样板与证据

`e2e/workbench-appearance.pw.ts` 使用真实临时服务、文件存储及浏览器，生成两种尺寸的 `notes.png` 和 `provider.png`。笔记样本使用真实 CTN 内容；Provider 为无明文密钥的测试对象。截图不代表真实模型服务验证。

同一测试文件验证控件键盘操作、分组折叠后输入保留、导航阻止、保存与放弃，以及问题面板关闭后不占编辑空间、重新打开后保留筛选。既有八活动布局与业务流程继续验证共享基础的调用方。

## 样板阶段验证结果（2026-09-06）

- 全量单元与集成测试：253 个文件、1275 项通过。旧折叠头部的静态交互断言已移入真实浏览器场景。
- Chromium：共 59 项场景。首次全量使用 2 个 worker，56 项通过；修正搜索状态和设置刷新按钮的定位后，与一次超时的 Agent 场景一起针对性复跑，3 项全部通过。Agent 超时发生在同时运行全量单元测试的首次运行中，独立复跑为 10.4 秒；未放宽超时门槛。
- 问题面板关闭后的焦点返回：补充浏览器断言，独立通过；筛选保留与编辑区高度同时验证。
- 两种尺寸的八活动布局、长名称、长路径、长列表和侧栏键盘调整通过。四张无敏感信息的样板截图已人工查看并留存到 `.artifacts/ui-dark-modern/{1280x720,1440x900}/`。样板测试原始输出位于 `.artifacts/test/playwright/`，后续可重新生成并留存。
- 前后端构建与包体积检查通过；保留 8 个活动懒加载，初始 JS 为 735427 字节，未调整门槛。
- 文档链接、锚点和命令入口检查通过。类型检查通过；架构检查 8 个文件、43 项通过，两者继续由每次提交的钩子执行。

本次后续验收覆盖容量对照、两个独立启动入口和运行包，以及八个活动的浏览器布局与流程。真实模型、其他浏览器/平台与真实断电不在当前样板验证证据范围内。

## 推广与运行环境分离（2026-09-09）

样板确认后，界面、服务、Profile、API 访问、迁移和默认会话表单统一采用 stacked 布局。
枚举复用 SelectControl；API 仓库授权与 Agent 资源成员复用 CheckboxGroup，内部只组合
CheckboxControl。FieldRow 统一处理成员组标题的 aria-labelledby，未新增配置状态所有者。

本轮最终全量单元与集成测试为 255 个文件、1287 项通过。Chromium 使用 2 个 worker，
原全量 59 项中 57 项通过；新增到两个尺寸场景的检查暴露了测试对可省略详情栏的等待、
以及主区与目录同名区域的定位问题。修正定位与实际尺寸读取后，该文件 4 项全部通过，
未放宽超时和容量门槛。八活动布局截图使用无敏感信息夹具并已查看。

完整 Git 历史迁往 repo-dev，可用目录 repo 从确定提交生成；两个目录的 start.sh
均不接受模式参数。包校验、备份安装与进程中断恢复流程见[部署与恢复](deployment.md)。

## 最终验收补充（2026-09-09）

- 类型检查、架构检查和既有提交钩子通过；前后端及 CLI 构建通过。保留 8 个活动懒加载，
  初始 JS 为 735858 字节，包体积门槛未调整。
- 容量对照使用同一 Node v22.18.0 和相同数据规模：1000 篇笔记、100000 个块。
  输出校验与验证调用次数一致；堆内存约 1.121 GB → 1.120 GB，RSS 约 1.299 GB →
  1.289 GB。各阶段计时合计 8.50 s → 9.36 s，主要差异来自文件提交；这是单次对照，
  不作为性能提升证据。原始输出保存在 `.artifacts/verification/capacity-{before,after}.txt`。
- 正式运行包来自 `c4c6a19c9125498ac01392ca80a6ddc41d314d66`，本地发布标签为
  `usable/20260909-c4c6a19`。仅显式发布更新可用版，开发提交不会改变它；未推送远端。
- 安装前完整归档并逐文件核验旧工作区，共 19114 个条目。正式数据原位保留，首次启动前
  225 个条目的字节、权限及修改时间一致，数据根目录身份不变。归档位于项目外层的
  `runtime-backups/initial-source-20260908T214104Z/original-repo.tar`，目录保持私有权限。
- 安装首次检查发现复制相对依赖链接会改变链接目标，在替换正式运行文件前拒绝继续。
  修正后，打包、安装、备份、恢复和隔离启动共用保留链接的复制实现。12 项运行环境测试
  包含真实链接、候选包删除、复制失败、校验失败以及安装子进程中断后的重复恢复。
- 新包在只提供 Node 和系统 Shell 的环境中通过 HTTP、Chromium、CLI、Codex 可执行文件
  与私有工具入口检查。复制后的完整性再次验证，避免依赖源目录掩盖缺失依赖。
- 实际两个 `./start.sh` 同时启动成功；清理开发构建、停止开发服务、移动候选包后，
  可用版仍正常响应。正式三领域快照通过 API v4 解码，两个实际工作台通过 Chromium 检查。
  正式数据不截图、不录制 trace；业务编辑流程仍由隔离夹具验证。
- 启动及读取后，145 个正式文件完全不变；另一个文件索引仅重新排列序列化条目并更新
  一处 inode 缓存。按路径核对的业务身份、顺序、内容摘要及其他字段均不变；正文及业务
  时间戳未改动。两个验证进程均已正常退出，端口与写入锁释放，可按需再次启动。

最终日志与不含敏感信息的统计保存在 `.artifacts/verification/`。真实模型服务、其他
浏览器与平台、真实断电仍未验证；进程中断测试仅提供进程恢复证据。
