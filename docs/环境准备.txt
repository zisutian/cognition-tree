环境准备


文档职责：

    只记录当前开发者本机环境、双机分工和待确认项。
    不记录外部使用说明、产品需求、工程架构、当前任务和长期流程规则。


1. Linux

用途：

    日常开发、测试、后端服务、Docker 验证、Git 提交。

状态：

    系统：Ubuntu 24.04.4 LTS
    项目目录：/home/zisu/code/cognition-tree/repo
    Git：2.43.0
    Node.js：v22.18.0
    npm：10.9.3
    pnpm：11.1.3
    Docker：28.3.3
    Docker Compose：v2.39.1
    SQLite：3.45.1


2. Windows

用途：

    拉取代码，通过浏览器访问同一个后端服务，检查界面、输入法、路径和数据互通。

当前目录：

    C:\Users\thefu\Documents\cognition-tree

待确认：

    独立 Node.js LTS
    pnpm
    Docker Desktop


3. 双机原则

    Windows 和 Linux 可以各自保留一份代码仓库。
    笔记数据互通应通过同一个 HTTP 后端服务完成。
    不依赖同步代码目录来同步笔记数据。
    主项目避免长期放在中文路径、空格路径或同步盘特殊目录下。
    仓库统一使用 LF；Windows 批处理和 PowerShell 文件保留 CRLF。
